const db = require('./config/db'); async function hunt() { try { const [tables] = await db.query('SELECT TABLE_NAME FROM information_schema.COLUMNS WHERE COLUMN_NAME = \'project_id\' AND TABLE_SCHEMA = DATABASE() AND TABLE_NAME NOT LIKE \'%view%\''); for (const row of tables) { const table = row.TABLE_NAME; try { const [res] = await db.query(\SELECT COUNT(*) as count FROM \ WHERE project_id IS NOT NULL AND project_id NOT IN (SELECT id FROM projects)\); if (res[0].count > 0) { console.log(\Zombie alert! Table \ has \ orphaned records.\); } } catch (err) { /* ignore views/errors */ } } console.log('Hunt complete.'); process.exit(0); } catch(e) { console.error(e); process.exit(1); } } hunt();
